# Day 1-2：项目脚手架与基础配置

## 任务概述
初始化 BetweenLines 项目的前后端仓库，安装所有依赖，搭建基础目录结构，配置开发环境。

**工作量：2 天**

---

## Day 1（上午）：项目初始化

### 任务 1.1：创建项目根目录结构

在 `/Users/tonghaobo/codes/chatvibe` 下创建以下目录结构：

```
chatvibe/
├── frontend/          # Next.js 前端
├── backend/           # FastAPI 后端
├── plans/             # 计划文档（已存在）
└── README.md          # 项目说明
```

**执行步骤：**

```bash
cd /Users/tonghaobo/codes/chatvibe
mkdir -p frontend backend
```

### 任务 1.2：初始化 Next.js 前端项目

在 `frontend/` 目录下初始化 Next.js 项目：

```bash
cd /Users/tonghaobo/codes/chatvibe/frontend
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack --use-npm
```

参数说明：
- `--typescript`：使用 TypeScript
- `--tailwind`：集成 Tailwind CSS
- `--eslint`：代码规范
- `--app`：使用 App Router
- `--src-dir`：使用 src 目录
- `--import-alias "@/*"`：路径别名

### 任务 1.3：初始化 FastAPI 后端项目

在 `backend/` 目录下创建 FastAPI 项目：

```bash
cd /Users/tonghaobo/codes/chatvibe/backend
mkdir -p app/routers app/services app/models app/schemas
```

创建 `backend/requirements.txt`：
```
fastapi==0.115.6
uvicorn[standard]==0.34.0
openai==1.59.3
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
```

创建 `backend/app/__init__.py`（空文件）

创建 `backend/app/main.py`：
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(
    title="BetweenLines API",
    description="BetweenLines - AI-powered chat analysis and reply suggestions",
    version="0.1.0",
)

# CORS 配置 - 允许前端跨域请求
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # V1 阶段允许所有来源
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/")
async def root():
    return {"message": "BetweenLines API is running"}

@app.get("/health")
async def health_check():
    return {"status": "healthy"}
```

创建 `backend/app/routers/__init__.py`（空文件）
创建 `backend/app/services/__init__.py`（空文件）
创建 `backend/app/models/__init__.py`（空文件）
创建 `backend/app/schemas/__init__.py`（空文件）

---

## Day 1（下午）：环境配置

### 任务 1.4：前端环境变量配置

创建 `frontend/.env.local`：
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

### 任务 1.5：后端环境变量配置

创建 `backend/.env`：
```
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
ALLOWED_ORIGINS=http://localhost:3000
```

**注意：** `OPENAI_API_KEY` 需要用户后续填入真实的 API Key。

### 任务 1.6：验证环境

**前端验证：**
```bash
cd /Users/tonghaobo/codes/chatvibe/frontend
npm run dev
# 访问 http://localhost:3000 确认 Next.js 默认页面正常显示
```

**后端验证：**
```bash
cd /Users/tonghaobo/codes/chatvibe/backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
# 访问 http://localhost:8000 确认返回 {"message": "BetweenLines API is running"}
# 访问 http://localhost:8000/docs 确认 Swagger UI 正常
```

---

## Day 2（上午）：前端基础页面框架

### 任务 2.1：清理前端默认文件

删除 Next.js 默认生成的无用内容：
- 清空 `frontend/src/app/page.tsx` 内容（保留文件）
- 删除 `frontend/src/app/globals.css` 中的默认样式，替换为基础样式
- 保留 `frontend/src/app/layout.tsx`

### 任务 2.2：设置全局样式

编辑 `frontend/src/app/globals.css`，替换为：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --primary: 221.2 83.2% 53.3%;
    --primary-foreground: 210 40% 98%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --border: 214.3 31.8% 91.4%;
  }

  body {
    @apply bg-white text-gray-900 antialiased;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  }
}

@layer components {
  .btn-primary {
    @apply bg-blue-600 text-white px-6 py-3 rounded-xl font-semibold 
           hover:bg-blue-700 active:scale-[0.98] transition-all duration-200 
           disabled:opacity-50 disabled:cursor-not-allowed shadow-sm;
  }
  
  .card {
    @apply bg-white border border-gray-100 rounded-2xl p-6 shadow-sm;
  }

  .tag-positive {
    @apply bg-green-50 text-green-700 px-3 py-1 rounded-full text-sm font-medium;
  }
  
  .tag-neutral {
    @apply bg-yellow-50 text-yellow-700 px-3 py-1 rounded-full text-sm font-medium;
  }
  
  .tag-negative {
    @apply bg-red-50 text-red-700 px-3 py-1 rounded-full text-sm font-medium;
  }
}
```

### 任务 2.3：配置布局组件

编辑 `frontend/src/app/layout.tsx`：

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "BetweenLines - Understand the vibe before you reply",
  description: "Paste your chat and get instant insight.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-gradient-to-b from-gray-50 to-white">
        <main className="mx-auto max-w-2xl px-4 py-8">
          {children}
        </main>
      </body>
    </html>
  );
}
```

### 任务 2.4：创建首页占位内容

编辑 `frontend/src/app/page.tsx`，创建基础占位：

```tsx
export default function Home() {
  return (
    <div className="flex flex-col items-center justify-center min-h-[80vh] text-center space-y-6">
      <h1 className="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
        Understand the vibe
        <br />
        before you reply.
      </h1>
      <p className="text-lg text-gray-500 max-w-md">
        Paste your chat and get instant insight.
      </p>
      <div className="w-full max-w-lg space-y-4">
        <textarea
          placeholder="Paste your chat here..."
          className="w-full h-40 p-4 border border-gray-200 rounded-xl resize-none 
                     focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent
                     text-gray-700 placeholder:text-gray-400"
        />
        <button className="btn-primary w-full">
          Analyze Chat
        </button>
      </div>
    </div>
  );
}
```

---

## Day 2（下午）：后端 API 基础路由与 Schema 定义

### 任务 2.5：定义数据模型

创建 `backend/app/schemas/chat.py`：

```python
from pydantic import BaseModel, Field
from typing import List, Optional
from enum import Enum


class ChatStatus(str, Enum):
    POSITIVE = "积极互动"
    NORMAL = "普通互动"
    POLITE = "礼貌回应"
    COLD = "偏冷淡"
    HIGH_RISK = "对话风险较高"


class ReplyStyle(str, Enum):
    NATURAL = "natural"
    HUMOROUS = "humorous"
    MATURE = "mature"


# 请求 Schema
class ChatAnalysisRequest(BaseModel):
    chat_content: str = Field(
        ..., 
        min_length=10, 
        max_length=5000,
        description="用户粘贴的聊天记录，支持微信格式"
    )


# 响应 Schema
class ReplySuggestions(BaseModel):
    natural: str = Field(..., description="自然版回复")
    humorous: str = Field(..., description="幽默版回复")
    mature: str = Field(..., description="成熟版回复")


class ChatAnalysisResponse(BaseModel):
    chat_status: ChatStatus = Field(..., description="当前互动状态")
    analysis: str = Field(..., description="互动分析描述")
    issues: List[str] = Field(default_factory=list, description="发现的聊天问题")
    risks: List[str] = Field(default_factory=list, description="风险提醒")
    reply_suggestions: ReplySuggestions = Field(..., description="回复建议")
    timing_advice: str = Field(..., description="节奏建议")


class FeedbackRequest(BaseModel):
    analysis_id: Optional[str] = Field(None, description="分析记录ID")
    helpful: bool = Field(..., description="是否有帮助")


class FeedbackResponse(BaseModel):
    message: str = "感谢你的反馈！"
```

### 任务 2.6：创建 API 路由骨架

创建 `backend/app/routers/chat.py`：

```python
from fastapi import APIRouter, HTTPException
from app.schemas.chat import (
    ChatAnalysisRequest,
    ChatAnalysisResponse,
    FeedbackRequest,
    FeedbackResponse,
)

router = APIRouter(prefix="/api/v1", tags=["chat"])


@router.post("/analyze", response_model=ChatAnalysisResponse)
async def analyze_chat(request: ChatAnalysisRequest):
    """
    分析聊天记录，返回互动状态、分析、问题和回复建议。
    """
    # TODO: Day 5-6 集成 OpenAI
    raise HTTPException(status_code=501, detail="Not implemented yet")


@router.post("/feedback", response_model=FeedbackResponse)
async def submit_feedback(request: FeedbackRequest):
    """
    收集用户反馈（有帮助/没帮助）。
    """
    # TODO: Day 9-10 实现反馈收集
    return FeedbackResponse(message="感谢你的反馈！")
```

### 任务 2.7：注册路由到主应用

更新 `backend/app/main.py`，在文件末尾 `app = FastAPI(...)` 之后添加：

```python
from app.routers import chat

app.include_router(chat.router)
```

### 任务 2.8：验证 API Schema

启动后端服务后，访问 `http://localhost:8000/docs`，确认：
- `/api/v1/analyze` 接口可见
- `/api/v1/feedback` 接口可见
- Schema 定义正确展示
- 可以在 Swagger UI 中测试请求

---

## 验收标准（Day 2 结束）

- [ ] `frontend/` 目录下 Next.js 项目可以 `npm run dev` 正常启动
- [ ] `backend/` 目录下 FastAPI 项目可以 `uvicorn` 正常启动
- [ ] 访问 `http://localhost:3000` 可以看到包含标题、输入框和按钮的首页
- [ ] 访问 `http://localhost:8000/docs` 可以看到 Swagger 文档
- [ ] `/api/v1/analyze` 和 `/api/v1/feedback` 接口已定义
- [ ] `.env` 和 `.env.local` 配置文件已创建
- [ ] 项目 git 已初始化（`git init`），`.gitignore` 已排除 `.env` 文件

## 给 AI 的执行提示

```
请按照以上步骤，依次完成 Day 1 和 Day 2 的所有任务。
先创建目录结构和初始化项目，再配置环境变量，最后编写代码文件。
每完成一个步骤后，运行验证命令确认该步骤成功。
如果遇到依赖安装问题，请尝试使用 --legacy-peer-deps 或调整版本号。
```
