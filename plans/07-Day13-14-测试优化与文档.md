# Day 13-14：测试优化与文档

## 任务概述
端到端测试、Prompt 调优、边界情况处理、性能优化、上线最终确认。

**工作量：2 天**

---

## Day 13（上午）：端到端测试用例

### 任务 10.1：创建前端 E2E 测试（Playwright）

安装 Playwright：

```bash
cd /Users/tonghaobo/codes/chatvibe/frontend
npm install -D @playwright/test
npx playwright install
```

创建 `frontend/tests/e2e/home.spec.ts`：

```typescript
import { test, expect } from "@playwright/test";

test.describe("BetweenLines Homepage", () => {
  test("should render hero section", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    // 标题
    await expect(page.locator("h1")).toContainText("Understand the vibe");
    
    // 输入框
    await expect(page.locator("textarea")).toBeVisible();
    
    // 按钮
    await expect(page.locator("button", { hasText: "Analyze Chat" })).toBeVisible();
  });

  test("should show validation for empty input", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    // 输入少于 10 字符
    await page.locator("textarea").fill("hi");
    await expect(page.locator("button", { hasText: "Analyze Chat" })).toBeDisabled();
  });

  test("should show validation for short input", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    // 输入少于 10 字符
    await page.locator("textarea").fill("hi");
    await expect(page.locator("text=at least 10")).toBeVisible();
  });

  test("should enable button with valid input", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const validChat = "A: hi\nB: hello there\nA: how are you\nB: good thanks";
    await page.locator("textarea").fill(validChat);
    
    await expect(page.locator("button", { hasText: "Analyze Chat" })).toBeEnabled();
  });

  test("should show character count", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const text = "A: hello\nB: hi";
    await page.locator("textarea").fill(text);
    
    await expect(page.locator(`text=${text.length}/5000`)).toBeVisible();
  });
});
```

创建 `frontend/tests/e2e/result.spec.ts`：

```typescript
import { test, expect } from "@playwright/test";

test.describe("BetweenLines Result Page", () => {
  test("should show loading state on submit", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const chat = "A: 今天在干嘛呀\nB: 刚下班，好累哈哈\nA: 辛苦啦\nB: 还没吃饭呢";
    await page.locator("textarea").fill(chat);
    await page.locator("button", { hasText: "Analyze Chat" }).click();
    
    // Loading 状态
    await expect(page.locator("text=Analyzing your chat")).toBeVisible({ timeout: 5000 });
  });

  test("should display result after analysis", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const chat = "A: 今天在干嘛呀\nB: 刚下班，好累哈哈\nA: 辛苦啦，吃饭了吗\nB: 还没呢\nA: 那快去吃点东西吧\nB: 嗯嗯好的";
    await page.locator("textarea").fill(chat);
    await page.locator("button", { hasText: "Analyze Chat" }).click();
    
    // 等待结果（AI 调用可能需要一些时间）
    await expect(page.locator("text=Chat Status")).toBeVisible({ timeout: 60000 });
    
    // 状态标签
    await expect(page.locator("text=积极互动")).toBeVisible({ timeout: 5000 })
      .or(page.locator("text=普通互动")).toBeVisible({ timeout: 5000 })
      .or(page.locator("text=礼貌回应")).toBeVisible({ timeout: 5000 });
    
    // 回复建议
    await expect(page.locator("text=Suggested Replies")).toBeVisible({ timeout: 5000 });
    
    // 自然版回复
    await expect(page.locator("text=自然版")).toBeVisible({ timeout: 5000 });
    
    // 节奏建议
    await expect(page.locator("text=Timing Advice")).toBeVisible({ timeout: 5000 });
  });

  test("should copy reply to clipboard", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const chat = "A: 今天在干嘛呀\nB: 刚下班，好累哈哈\nA: 辛苦啦\nB: 还没吃饭呢";
    await page.locator("textarea").fill(chat);
    await page.locator("button", { hasText: "Analyze Chat" }).click();
    
    // 等待结果
    await expect(page.locator("text=自然版")).toBeVisible({ timeout: 60000 });
    
    // 点击 Copy 按钮
    const copyButton = page.locator("button", { hasText: "Copy" }).first();
    await copyButton.click();
    
    // 验证 "Copied" 状态
    await expect(page.locator("text=✓ Copied").first()).toBeVisible({ timeout: 3000 });
  });

  test("should navigate back to home", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const chat = "A: hi\nB: hello\nA: how are you\nB: good, you?";
    await page.locator("textarea").fill(chat);
    await page.locator("button", { hasText: "Analyze Chat" }).click();
    
    // 等待结果
    await expect(page.locator("text=Chat Status")).toBeVisible({ timeout: 60000 });
    
    // 点击返回
    await page.locator("text=Analyze another chat").click();
    
    // 回到首页
    await expect(page.locator("h1")).toContainText("Understand the vibe");
  });

  test("should submit feedback", async ({ page }) => {
    await page.goto("http://localhost:3000");
    
    const chat = "A: hi\nB: hello\nA: how are you\nB: good, you?";
    await page.locator("textarea").fill(chat);
    await page.locator("button", { hasText: "Analyze Chat" }).click();
    
    // 等待结果
    await expect(page.locator("text=Was this analysis helpful?")).toBeVisible({ timeout: 60000 });
    
    // 点击有帮助
    await page.locator("button", { hasText: "Helpful" }).click();
    
    // 验证反馈已提交
    await expect(page.locator("text=Thanks for your feedback")).toBeVisible({ timeout: 5000 });
  });
});
```

创建 `frontend/playwright.config.ts`：

```typescript
import { defineConfig } from "@playwright/test";

export default defineConfig({
  testDir: "./tests/e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: "html",
  use: {
    baseURL: "http://localhost:3000",
    trace: "on-first-retry",
  },
  webServer: {
    command: "npm run dev",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 120000,
  },
});
```

---

## Day 13（下午）：Prompt 调优

### 任务 10.2：Prompt 测试与优化

创建 `backend/tests/test_prompt.py`：

```python
"""
Prompt 效果测试脚本
用于测试不同聊天场景下的 AI 分析质量
"""
import os
import json
import asyncio
from openai import AsyncOpenAI
from dotenv import load_dotenv

load_dotenv()

client = AsyncOpenAI(api_key=os.getenv("OPENAI_API_KEY"))
MODEL = os.getenv("OPENAI_MODEL", "gpt-4o-mini")

SYSTEM_PROMPT = """你是一个专业的社交沟通分析助手。

你的目标：
帮助用户理解聊天状态，并给出自然沟通建议。

分析重点：
1. 互动积极程度
2. 对方主动性
3. 情绪反馈
4. 潜在聊天问题
5. 风险提醒
6. 回复建议

禁止：
• 判断喜欢程度
• 编造事实
• 情绪操控
• PUA 风格
• 极端两性观点

回复必须：
自然、现实、可执行。

回复建议规则：
- 每条回复不超过 2 句话
- 自然、可直接复制发送
- 不油腻、不尴尬
- 禁止套路话术

输出格式：
严格 JSON。"""

# 测试场景
TEST_CASES = [
    {
        "name": "积极互动",
        "chat": """A: 今天在干嘛呀
B: 刚下班，好累哈哈
A: 辛苦啦，吃饭了吗
B: 还没呢，等下去吃火锅！
A: 哇火锅，哪家呀
B: 就公司楼下那家，超好吃的
A: 我也想去！""",
        "expected_status": "积极互动",
        "checks": ["有情绪反馈", "对方主动延伸话题", "回复建议自然"],
    },
    {
        "name": "冷淡回应",
        "chat": """A: 周末有什么安排吗
B: 没有
A: 最近有部电影还不错
B: 哦
A: 你喜欢看电影吗
B: 还行""",
        "expected_status": "偏冷淡",
        "checks": ["指出回复简短", "建议暂停追问", "不判断喜欢程度"],
    },
    {
        "name": "礼貌回应",
        "chat": """A: 今天工作忙吗
B: 还行，就那样吧
A: 我也是，最近项目挺多的
B: 嗯嗯，辛苦了""",
        "expected_status": "礼貌回应",
        "checks": ["对方有回应但被动", "建议增加情绪价值", "回复建议不油腻"],
    },
    {
        "name": "高风险对话",
        "chat": """A: 你觉得我怎么样
B: 什么怎么样
A: 就是你觉得我这个人
B: 还行吧
A: 那我们是什么关系
B: 朋友啊
A: 只是朋友吗""",
        "expected_status": "对话风险较高",
        "checks": ["指出话题推进过快", "风险提醒明确", "不建议升级关系"],
    },
]


async def test_case(case: dict):
    """测试单个场景"""
    user_prompt = f"""请分析以下聊天记录。

聊天内容：
---
{case['chat']}
---

请以 JSON 格式输出分析结果。JSON schema 如下：
{{"chat_status": "...", "analysis": "...", "issues": [...], "risks": [...], "reply_suggestions": {{"natural": "...", "humorous": "...", "mature": "..."}}, "timing_advice": "..."}}"""

    response = await client.chat.completions.create(
        model=MODEL,
        messages=[
            {"role": "system", "content": SYSTEM_PROMPT},
            {"role": "user", "content": user_prompt},
        ],
        temperature=0.7,
        max_tokens=1000,
        response_format={"type": "json_object"},
    )

    content = response.choices[0].message.content
    result = json.loads(content)

    print(f"\n{'=' * 60}")
    print(f"📋 场景: {case['name']}")
    print(f"🎯 期望状态: {case['expected_status']}")
    print(f"📊 实际状态: {result.get('chat_status')}")
    print(f"📝 分析: {result.get('analysis', '')[:100]}...")
    print(f"⚠️  问题: {result.get('issues', [])}")
    print(f"🚨 风险: {result.get('risks', [])}")
    print(f"💬 自然版回复: {result.get('reply_suggestions', {}).get('natural', '')}")
    print(f"⏱️  节奏建议: {result.get('timing_advice', '')}")

    # 检查项
    for check in case.get("checks", []):
        print(f"   ✅ 检查项: {check}")

    return result


async def main():
    print("=" * 60)
    print("BetweenLines Prompt 效果测试")
    print("=" * 60)

    results = []
    for case in TEST_CASES:
        try:
            result = await test_case(case)
            results.append({"case": case["name"], "status": "passed", "result": result})
        except Exception as e:
            print(f"\n❌ {case['name']} 测试失败: {str(e)}")
            results.append({"case": case["name"], "status": "failed", "error": str(e)})

    # 汇总
    print(f"\n{'=' * 60}")
    print("📊 测试汇总")
    print(f"{'=' * 60}")
    passed = sum(1 for r in results if r["status"] == "passed")
    failed = sum(1 for r in results if r["status"] == "failed")
    print(f"通过: {passed}/{len(results)}")
    print(f"失败: {failed}/{len(results)}")


if __name__ == "__main__":
    asyncio.run(main())
```

### 任务 10.3：Prompt 调优记录

创建 `backend/docs/prompt-tuning.md`：

```markdown
# Prompt 调优记录

## 当前版本：V1

### System Prompt 设计原则
1. 明确角色定位（社交沟通分析助手）
2. 列出分析重点（6 项）
3. 明确禁止事项（5 项）
4. 设定回复风格规则（4 条）
5. 严格输出格式（JSON）

### 测试结果
| 场景 | 期望状态 | 实际状态 | 是否准确 |
|------|---------|---------|---------|
| 积极互动 | 积极互动 | TBD | - |
| 冷淡回应 | 偏冷淡 | TBD | - |
| 礼貌回应 | 礼貌回应 | TBD | - |
| 高风险对话 | 对话风险较高 | TBD | - |

### 调优方向（如需要）
- [ ] 温度参数调整（当前 0.7）
- [ ] System Prompt 措辞优化
- [ ] JSON Schema 约束加强
- [ ] Few-shot 示例添加
```

---

## Day 14（上午）：性能优化

### 任务 10.4：前端性能优化

1. **图片/字体优化**：本 V1 项目无图片和自定义字体，跳过。

2. **代码分割检查**：Next.js 自动按路由分割代码，确认 `next.config` 无需额外配置。

3. **API 响应缓存提示**（可选）：

创建 `frontend/src/lib/cache.ts`：

```typescript
/**
 * 简单的客户端缓存工具（V1 可选）
 * 对相同聊天内容在 5 分钟内返回缓存结果，减少 API 调用
 */

interface CacheEntry {
  data: unknown;
  timestamp: number;
}

const cache = new Map<string, CacheEntry>();
const CACHE_TTL = 5 * 60 * 1000; // 5 分钟

function hashString(str: string): string {
  let hash = 0;
  for (let i = 0; i < str.length; i++) {
    const char = str.charCodeAt(i);
    hash = ((hash << 5) - hash) + char;
    hash = hash & hash;
  }
  return hash.toString(36);
}

export function getCached(key: string): unknown | null {
  const hashedKey = hashString(key);
  const entry = cache.get(hashedKey);
  
  if (!entry) return null;
  
  if (Date.now() - entry.timestamp > CACHE_TTL) {
    cache.delete(hashedKey);
    return null;
  }
  
  return entry.data;
}

export function setCache(key: string, data: unknown): void {
  const hashedKey = hashString(key);
  cache.set(hashedKey, { data, timestamp: Date.now() });
}
```

### 任务 10.5：后端性能优化

1. **OpenAI 调用优化**：
   - 使用 `gpt-4o-mini`（已在配置中设置，性价比高）
   - `max_tokens` 限制为 1000（已设置）
   - 考虑添加响应流式传输（V2）

2. **添加请求耗时日志**：

在 `backend/app/services/openai_service.py` 的 `analyze_chat` 方法中已通过路由层记录耗时（Day 10 已实现）。

3. **SQLite 连接池**（可选优化）：

更新 `backend/app/services/storage.py`，确保连接及时关闭：

```python
import sqlite3
from contextlib import contextmanager

@contextmanager
def get_db_connection():
    """使用上下文管理器确保连接关闭"""
    conn = sqlite3.connect(str(DB_PATH))
    conn.row_factory = sqlite3.Row
    try:
        yield conn
    finally:
        conn.close()
```

---

## Day 14（下午）：最终验收与上线

### 任务 10.6：最终验收测试清单

运行完整测试套件：

```bash
# 1. 后端单元测试
cd /Users/tonghaobo/codes/chatvibe/backend
python tests/test_api.py

# 2. Prompt 效果测试
python tests/test_prompt.py

# 3. 前端 E2E 测试
cd /Users/tonghaobo/codes/chatvibe/frontend
npx playwright test

# 4. 生产环境验证（部署后运行）
PROD_URL=https://your-api.railway.app python backend/tests/test_production.py
```

### 任务 10.7：最终文件结构确认

确保项目最终结构如下：

```
chatvibe/
├── README.md
├── .gitignore
├── plans/
│   ├── 00-总览-产品交付计划总览.md
│   ├── 01-Day1-2-项目脚手架与基础配置.md
│   ├── 02-Day3-4-前端首页UI开发.md
│   ├── 03-Day5-6-后端API与AI集成.md
│   ├── 04-Day7-8-前端结果页与交互.md
│   ├── 05-Day9-10-前后端联调与反馈功能.md
│   ├── 06-Day11-12-部署与上线准备.md
│   ├── 07-Day13-14-测试优化与文档.md
│   └── deployment-checklist.md
├── frontend/
│   ├── package.json
│   ├── next.config.mjs
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── vercel.json
│   ├── playwright.config.ts
│   ├── .env.local
│   ├── src/
│   │   ├── app/
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx
│   │   │   ├── globals.css
│   │   │   ├── robots.ts
│   │   │   └── sitemap.ts
│   │   ├── components/
│   │   │   ├── hero/
│   │   │   │   ├── HeroSection.tsx
│   │   │   │   └── ExampleChats.tsx
│   │   │   ├── chat-input/
│   │   │   │   └── ChatInput.tsx
│   │   │   ├── result/
│   │   │   │   ├── ResultPage.tsx
│   │   │   │   ├── StatusBadge.tsx
│   │   │   │   ├── AnalysisCard.tsx
│   │   │   │   ├── ReplyCard.tsx
│   │   │   │   ├── ReplySuggestions.tsx
│   │   │   │   └── TimingAdvice.tsx
│   │   │   ├── feedback/
│   │   │   │   └── FeedbackSection.tsx
│   │   │   └── ui/
│   │   │       ├── LoadingOverlay.tsx
│   │   │       ├── SkeletonCard.tsx
│   │   │       └── EmptyState.tsx
│   │   └── lib/
│   │       ├── api.ts
│   │       ├── useChatAnalysis.ts
│   │       └── cache.ts
│   └── tests/
│       └── e2e/
│           ├── home.spec.ts
│           └── result.spec.ts
├── backend/
│   ├── requirements.txt
│   ├── Procfile
│   ├── runtime.txt
│   ├── .env
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py
│   │   ├── core/
│   │   │   ├── __init__.py
│   │   │   └── config.py
│   │   ├── routers/
│   │   │   ├── __init__.py
│   │   │   └── chat.py
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── openai_service.py
│   │   │   ├── chat_cleaner.py
│   │   │   └── storage.py
│   │   ├── schemas/
│   │   │   ├── __init__.py
│   │   │   └── chat.py
│   │   └── middleware/
│   │       ├── __init__.py
│   │       ├── rate_limit.py
│   │       └── security.py
│   ├── tests/
│   │   ├── __init__.py
│   │   ├── test_api.py
│   │   ├── test_prompt.py
│   │   └── test_production.py
│   └── docs/
│       └── prompt-tuning.md
```

### 任务 10.8：创建 .gitignore

确保根目录 `.gitignore` 包含：

```gitignore
# Dependencies
node_modules/
__pycache__/
*.py[cod]

# Environment
.env
.env.local
.env.*.local

# Build output
.next/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Database
*.db
*.sqlite

# Logs
*.log

# Test
playwright-report/
test-results/

# Python
*.egg-info/
venv/
.venv/
```

### 任务 10.9：提交 Git

```bash
cd /Users/tonghaobo/codes/chatvibe
git init
git add .
git commit -m "feat: BetweenLines MVP - initial commit

- Next.js frontend with chat analysis UI
- FastAPI backend with OpenAI integration
- SQLite feedback storage
- Deployment configs for Vercel & Railway
- E2E tests with Playwright
- Prompt tuning test suite"
```

---

## 验收标准（Day 14 结束）

- [ ] 所有 Playwright E2E 测试通过
- [ ] Prompt 效果测试通过（4 个场景均输出合理结果）
- [ ] 前后端性能可接受（API 响应 < 10s，页面加载 < 3s）
- [ ] 文件结构完整、无遗漏
- [ ] `.gitignore` 正确配置（API Key 不提交）
- [ ] 代码已 Git 提交
- [ ] 生产环境可正常访问
- [ ] PRD 中 V1 的三个核心功能全部可用
- [ ] PRD 中的产品原则全部遵守

## 给 AI 的执行提示

```
请按照以上步骤，依次完成 Day 13 和 Day 14 的所有任务。
Playwright 测试需要先安装浏览器驱动：npx playwright install。
Prompt 测试需要 OPENAI_API_KEY 已配置。
E2E 测试中的 AI 调用测试可能耗时较长（等待 AI 响应），超时已设置为 60s。
最终文件结构确认请对照实际创建的文件逐一核对。
.gitignore 非常重要——确保 .env 和数据库文件不会被提交。
```
