---
description: ChatVibe 核心开发规范（始终加载）
alwaysApply: true
enabled: true
updatedAt: 2026-06-14T09:41:00.000Z
provider: 
---

# ChatVibe 核心规范

> 目标：防止混乱代码、过度设计、功能膨胀、架构漂移

---

# 0. 核心原则（最高优先级）

## P0：可运行优先
- 代码必须可运行，不允许伪代码或"理论设计"替代实现

## P0：最小功能原则
- 只实现当前任务要求，不允许提前开发未来功能，不允许"顺手优化"

## P0：禁止复杂化
- 禁止过度抽象、设计模式炫技、多层无意义封装

## P0：需求不明先问
- 需求不明确时必须先问，不允许猜测实现

## P0：AI 应评估用户建议，而非盲从
- 当用户提出修改建议/诉求时，AI 必须基于项目架构、设计原则、产品红线自行评估其合理性
- **允许拒绝**：如果建议违背项目规范（技术栈锁定、产品红线、代码质量规则、目录结构约束等），应直接拒绝，并给出替代方案
- **允许协商**：如果建议部分合理但实现方式有更好的选择，应先说明利弊再提供替代方案
- **不得盲从**：禁止不做判断直接按用户建议执行，即使建议来自项目作者
- 示例：用户说"把界面写花哨一点" → AI 应拒绝，理由是项目倾向简约风格，而非堆砌动效
- 示例：用户建议引入 Redux → AI 应拒绝，理由是项目规则已锁定不使用状态管理库
- 示例：用户要求删除某个模块 → AI 应确认必要性，而非直接删除

---

# 1. 任务执行规范

每次任务严格遵循：**Think small → Build small → Verify fast**

如需求明确，按以下结构输出：
- **Goal**：当前任务目标
- **Plan**：简要步骤（3~6 条）
- **Files**：将修改/创建的文件清单
- **Implementation**：完整代码
- **Run**：如何运行
- **Verify**：如何验证

---

# 2. 单任务规则（强制执行）

## 禁止
- 同时开发多个 feature
- 自行扩展需求
- 添加未要求页面/API
- 提前做 V2/V3 功能（反馈系统按 Phase 渐进，不跳阶段）
- 重构整个项目结构
- 删除已有模块（除非用户明确要求）

## 必须
- 只解决当前 task
- 发现问题 → 记录，不修改范围
- 需新增功能 → 必须先询问用户

---

# 3. 产品红线（任何改动不得违反）

| 规则 | 说明 |
|------|------|
| ❌ 不做绝对判断 | 不说"她喜欢你"等确定性结论 |
| ❌ 不代替用户聊天 | 不自动发送消息 |
| ❌ 不制造焦虑 | 禁止 PUA 风格，不恐吓用户 |
| ❌ 不训练模型 | V1 直接调用 API，不使用用户数据训练 |
| ✅ 聊天记录不存储 | 仅内存处理，不落盘 |
| ✅ 输出自然可发送 | 回复建议可直接复制使用 |

---

# 4. 错误处理强制规则

所有系统必须包含：
- try/catch
- loading state
- error state
- timeout fallback

**禁止页面白屏**

---

# 5. 代码质量规则

## 必须
- TypeScript 类型完整
- 明确函数职责，可读性优先
- 函数不超过 100 行

## 禁止
- magic function、未命名变量
- 超长函数（>100 行）

---

# 6. 修改规则（严格）

## AI 不得
- 重构整个系统
- 删除已有模块
- 改变架构风格

## AI 只能
- 修改当前 task 相关文件

---

# 7. 成功标准

代码必须满足：
- 可运行
- 可部署
- 可测试
- 不依赖隐式逻辑

---

# 8. Git 提交与推送规则（强制）

- **禁止自动提交**：没有用户明确指示，不得执行 `git commit`
- **用户明确说「提交」「commit」等关键词时**才能执行 `git commit`
- **禁止自动推送**：没有用户明确指示，不得执行 `git push`
- **用户明确说「推送」「push」「提交到远程」等关键词时**才能执行 `git push`
- **唯一例外**：用户在同一轮对话中先说了「提交」再接着说「推送」，则可以连续执行

## 提交前强制流程

每次 commit 前必须完成两项检查：

### A. 回归验证（三级，Level 2/3 详见 `chatvibe-testing` 规则文件）

**Level 1 快速校验（每次提交必须，<5s，不消耗 AI Token）**：

改动涉及后端 → 运行校验测试（不调 AI）：
```
cd backend && python tests/test_comprehensive.py --no-ai
# 6/6 校验通过 = 过关
```

改动仅涉及前端 → 检查编译状态：
```
cd frontend && npx next build --no-lint 2>&1 | tail -5
# 无 error = 过关
```

> Level 2（核心 AI 场景）和 Level 3（全量回归）的详细规则见 `chatvibe-testing` 规则文件。

### B. 文档同步检查（核心义务，任何代码改动都适用）
⚠️ **任何代码改动提交前，必须检查是否需要同步更新文档**，不允许"稍后再补文档"。

**AI Agent 强制流程**：
1. `git diff --name-only` 查看变更文件清单
2. 根据映射表逐项检查（映射表见 `chatvibe-docs` 规则文件），列出需要更新的文档
3. 更新对应文档（如需要），告知用户更新了哪些文档
4. 将代码 + 文档一起 `git add` + `git commit`

> 映射表简记：API 端点→`04-backend` / 前端组件→`03-frontend` / 数据库表→`01-architecture`+`04-backend` / 配置变更→`01-setup` / 新功能→`00-overview`

---

# 9. 程序重启规则（强制）

- 凡是改动需要重启服务才能生效的（后端 FastAPI / 前端 Next.js），**自动在改动完成后重启对应程序**
- 后端：kill 旧进程 → 重新启动 uvicorn
- 前端：kill 旧进程 → 重新启动 next dev
- 注意：先确认当前是否有进程在运行，如有则重启；如无则直接启动
- 原则：改动代码 → 立刻让用户看到效果，不等到手动重启
