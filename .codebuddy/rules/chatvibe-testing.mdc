---
description: 测试与回归验证规则（触及测试文件时加载）
alwaysApply: false
enabled: true
updatedAt: 2026-06-14T09:41:00.000Z
provider: 
paths:
  - "backend/tests/**"
  - "**/*.test.*"
  - "**/*.spec.*"
---

# 提交前回归验证（Level 2/3 详细参考）

> ⚠️ Level 1 快速校验命令已在 `chatvibe-core` Section 8 中定义（始终生效）。本文件在 AI 触及测试文件时加载，提供 Level 2/3 详细流程。

## AI Agent 强制流程

### Level 2：核心场景（改动涉及 AI/分析逻辑时，按需运行，需确认）

⚠️ **此级别消耗 AI Token，必须先告知用户预估消耗，获得确认后再执行。**

仅运行 1~2 个代表性场景验证分析质量：

```
cd backend && python tests/test_comprehensive.py --test=解题模式_典型,偏冷淡
# 预估消耗：约 6k ~ 16k tokens（2 个场景 × 系统 Prompt + 响应）
# 验证目标：状态准确、无套话、无泛化兜底回复
```

> **禁止**在 Level 2 运行全量场景（15 个）。

### Level 3：全量回归（仅限重大改动、发版前，必须用 --ai 确认）

```
cd backend && python tests/test_comprehensive.py --ai
# 全量 15 个 AI 场景，预估消耗 45k ~ 120k tokens
# 通过率 ≥ 50%、耗时 <20s avg、无套话 100%、回复质量 100%
```

> **规则**：Level 3 仅限以下情况，且必须用户明确同意：
> - 修改 System Prompt / 分析逻辑
> - 发版前质量检查
> - 模型切换验证

## 测试脚本使用说明

| 命令 | 说明 | Token 消耗 |
|------|------|:---:|
| `--no-ai` | 仅 PHASE 1 校验（每次提交用） | 0 |
| `--test=场景1,场景2` | 指定 1~N 个 AI 场景 | 约 3k~8k/场景 |
| `--ai` | 全量 AI 场景（15 个） | 约 45k~120k |
| （无参数） | 校验 + 全量 AI（会打印警告） | 约 45k~120k |

## 验证结果处理

| 结果 | 处理 |
|------|------|
| ✅ 全部通过 | 继续提交流程 |
| ⚠️ 校验通过但核心场景失败 | 告知用户，询问是否继续提交 |
| ❌ 校验失败 | **阻塞提交**，先修复问题 |
| ⏱️ 超时无法完成 | 告知用户，降级为手动验证 |

## 可跳过场景

- 仅修改文档（`docs/`）、测试文件、配置文件
- 仅修改非运行时文件（`README.md`、`.gitignore`）
- 用户明确说「跳过测试直接提交」

## 验证记录

每次提交前验证结果应告知用户：通过/失败项、关键指标（耗时、质量评分）。
